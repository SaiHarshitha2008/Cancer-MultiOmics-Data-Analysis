# Results

The result files in this folder are generated by `analysis.py`.

Run the analysis after placing the real exported patient-level data in:

`data/egfr_survival.csv`

Do not type the p-value or survival values into the result file manually. They should come from the analysis.
